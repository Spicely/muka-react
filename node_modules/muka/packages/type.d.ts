declare const type: {
    form: (it: any) => boolean;
    file: (it: any) => boolean;
    array: (it: any) => boolean;
    object: (it: any) => boolean;
    func: (it: any) => boolean;
    string: (it: any) => boolean;
    blob: (it: any) => boolean;
    number: (it: any) => boolean;
    element: (it: any) => boolean;
    notVol: (it: any) => boolean;
    bool: (it: any) => boolean;
    emptyObject: (it: object) => boolean;
};
export default type;
